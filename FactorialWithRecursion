#include <iostream>

int Factorial(int x)
{    
    if(x<1) {return 1;}
    return x*Factorial(x-1);
}
int main()
{
  int x;
  std::cout<<" insert nuber : ";
  std::cin>>x;
  std::cout<<" The factorial of "<<x<< " is "<<Factorial(x);
   return 0;
}
